[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "asset_manager"
version = "0.1.0"
description = "A unified, simulator-agnostic registry for robotics assets"
requires-python = ">=3.13"
license = {text = "BSD-3-Clause"}
dependencies = [
    "pyyaml>=6.0.3",
]

[tool.hatchling.build]
packages = ["src/asset_manager"]
