[project]
name = "asset_manager"
version = "0.1.0"
description = "A unified, simulator-agnostic registry for robotics assets with support for multiple simulators, perception pipelines, and learning policies"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.3",
]
